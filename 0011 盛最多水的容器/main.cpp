#include <iostream>
#include <vector>
using namespace std;
 
class Solution {
public:
    int maxArea(vector<int>& height) {
        for (vector<int>::iterator it = height.begin(); it != height.end() ; it++)
        {
            
        }
        
    }
};